"""Vendored third-party code.

Currently contains an unmodified copy of the JTF (JSON Token Format) reference
encoder so token-diet can measure the real format without a runtime dependency
on the upstream repo. See ``jtf.py`` for source and license.
"""
